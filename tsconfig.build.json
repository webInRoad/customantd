{
  "compilerOptions": {
    "outDir": "build",//编译到build文件夹
    "module": "ESNext",//采用ES MOUDLE,而不是CommonJS,AMD,UMD(文件体积大，没办法按需加入)等
    "target": "ES5",//编译成ES 哪个版本
    "declaration": true,//是否要生成.d.ts声明文件
    "jsx": "react",//jsx语法糖转成React.createElement
    "moduleResolution":"Node",//有classic与Node,两者路径的查找方式不同
    "allowSyntheticDefaultImports": true,
    //设为true,就可以import React from 'react' 替换 import * as React from 'react'
    "skipLibCheck": true
  },
  "include": ["src"],
  "exclude": ["src/**/*.test.tsx","src/**/*.stories.tsx"],
}
